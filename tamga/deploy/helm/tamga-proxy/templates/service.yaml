apiVersion: v1
kind: Service
metadata:
  name: {{ include "tamga-proxy.fullname" . }}
  labels: {{- include "tamga-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "tamga-proxy.selectorLabels" . | nindent 4 }}
